{
  "development": {
    "username": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}"
  },
  "test": {
    "username": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "database_test",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}"
  },
  "production": {
    "username": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "database_production",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}"
  }
}
